source 'https://rubygems.org'

# Use the github-pages gem so local builds match the GitHub Pages production
# environment (Ruby 3.3.x, Jekyll 3.10.x, and the whitelisted plugins including
# jekyll-paginate and jekyll-sitemap). See https://pages.github.com/versions/
gem 'github-pages', group: :jekyll_plugins
